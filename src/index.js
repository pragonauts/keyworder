/*
 * @author David Menger
 */
'use strict';

const Network = require('./Network');
const activations = require('./activations');
const regularizations = require('./regularizations');
const Runner = require('./Runner');

module.exports = {

    Network,
    activations,
    regularizations,
    Runner

};
